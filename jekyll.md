---
layout: post
title: 개발 일지
permalink: /docs/devlog.html
date: 2026-08-25
categories: [dev-log, hackathon]
tags: [FastAPI, Next.js, Phaser, pgvector, RAG, DDD, Hexagonal]
---

> **프로젝트:** 라이프 튜토리얼 — AI 기반 게임형 실전 생활법률·금융 교육 플랫폼.
> 수능 직후 고3~사회초년생이 악덕 사장·중개인·보이스피싱범을 상대로 실전 리허설하며 노동·생활법률을 체득하는 AI 시뮬레이션 게임.
> **스택:** 백엔드 FastAPI + PostgreSQL(pgvector) / 프론트 Next.js 16 + React 19 + Phaser 4 + zustand + MSW.
> **아키텍처:** Hexagonal + Clean Architecture + DDD 기반 모듈러 모놀리스, "1 ERD 테이블 = 1 Fractal 11-File Set = 1 AI 위임 단위" 원칙으로 AI 하네스 엔지니어링.

<div class="mx-eli5">
  <div class="mx-eli5-title">처음 보는 분을 위한 3줄 안내</div>
  <ol>
    <li>이 문서는 개발팀의 날짜별 작업 일기다. 그날 무엇을 만들었고, 무엇이 실패했고, 어떻게 고쳤는지를 기록한다.</li>
    <li>v1, v2 같은 표기는 같은 것을 몇 번째 고친 버전인지이고, % 숫자는 시험(평가) 점수다. 수치의 전체 그림은 <a href="{{ '/docs/metrics.html' | relative_url }}">평가 지표 대시보드</a>에 모여 있다.</li>
    <li>낯선 용어(RAG, 방어전, 판정 등)는 <a href="{{ '/docs/glossary.html' | relative_url }}">용어 정의</a>에서 쉬운 말로 풀어두었다.</li>
  </ol>
</div>

---

## 2026-06-27 — 팀 구성

- 팀 프로필 문서 작성(`팀프로필.md`). 해커톤 예선 대비 팀 체계 정리.

## 2026-07-06 — 기획 문서 3종 확정 (v1.2)

- **PSST 사업계획서 v1.2**: 예비/초기창업패키지 표준 양식 기반. 교사용 대시보드 폐기 → 교사 관여 제로 B2G(기관 구매·바우처·자동 집계 리포트) 재구조화, 콜드스타트 해법을 또래 바이럴(결과 카드 공유·친구 대결)로 교체.
- **개발 정의서(DEVELOPMENT_SPEC) v1.2**: 기술 요구사항·ERD 초안·Decision Log(D-01~) 체계 수립.
- **UI/화면 정의서 v1.2**: 학생판 MVP 화면 설계(알바 연대기 챕터), 가드 촬영 품질 게이트, 또래·사전예약 화면.

## 2026-07-09 ~ 07-10 — 시스템 설계 다이어그램

- 시스템 아키텍처·플로우 다이어그램(drawio/png), MVP 플로우 별도 작성.
- 기술 스택 정리(`tech_life.md`), 워크플로 정리(`workflow_life.md`).

## 2026-07-28 ~ 07-31 — 예선 통과, 본선 대응 설계

- **해커톤 본선 대응 통합 정리서 v0.1** (7/28): 본선 일정이 '26.11 → **'26.9 첫 주로 당겨져 개발 기간 약 5주 확정**. 현장 로컬 평가 확정 → 웹 데모 필수(D-36). 심사 배점(참신성 30/구현 가능성 30/현장 실효성 30/보안성 10) 대응 전략과 검증 통계(임금체불 2조 678억, 청소년 알바 19.5% 근로계약서 미작성 등) 원출처 확보.
- **핵심 콘텐츠 설계서 4종** (7/31):
  - A1 퀘스트 상세설계서 — "근로계약서 쓰기" 퀘스트(대화 방어전 3단 방어선)
  - A2 판정 프롬프트 스펙 — LLM 판정 verdict 체계
  - A4 Fake 어댑터 데모 시나리오 — 오프라인 데모용 표준 시퀀스
  - A5 미니게임 콘텐츠
- 서비스 핵심 정리(데이터·게임·가드·리포트 4축) 문서화.

## 2026-08-02 — 백엔드 착수: core 인프라 + ontology BC (v0.1.0 ~ v0.5.0)

5주 실행 계획 v0.2 확정("3분 데모 시나리오에 나오는 것만 만든다" — 가드·리포트는 문서/목업으로 전환)과 함께 백엔드 개발 시작. 하루 만에 v0.1.0 → v0.5.0.

- **v0.1.0** — `core/matrix/` 전역 인프라 첫 파일: Seraph 인증 매니저(bcrypt 해싱 + JWT HS256, 테스트 7건).
- **v0.2.0~v0.2.1** — Keymaker 시크릿 매니저(시크릿 단일 서빙 지점), Oracle DB 매니저(공통 declarative Base + 트랜잭션 경계 세션), 의존성 정비.
- **v0.3.0** — 전 패키지 버전 고정, SQLModel 제거(Boundary Gate 원칙과 상충 → SQLAlchemy 2.0 단일화).
- **v0.4.0** — Alembic 마이그레이션 환경 구축(시크릿은 .env에서 주입, ini 미포함).
- **v0.5.0** — **ontology BC 첫 구현 (Fractal 파일 세트 풀 배선)**: 법제처 DRF API 법령 수집 게이트웨이 → Law/LawArticle/LawChunk 애그리거트 → pgvector cosine 검색까지. EmbeddingPort 어댑터 패턴(Gemini 온라인 / 로컬 오프라인 스텁, `.env` 한 줄로 전환). 근로기준법 132개 조문 → 284청크 수집·임베딩 E2E 실측. Docker Compose로 pgvector/pg17 DB(포트 5433) 기동.

## 2026-08-03 — 백엔드 데이터·판정 파이프라인 완성 (v0.6.0 ~ v0.9.0)

- **v0.6.0** — quest·npc BC 스캐폴딩(myself 엔드포인트 배선 검증, TDD). 시행령·시행규칙 추가 수집 → 총 11개 법령 1,051청크.
- **v0.7.0** — **로컬 임베딩 어댑터**(Qwen3-Embedding-4B, 1536차원 → 스키마 무변경) + provider별 벡터 공존 컬럼(온/오프라인 전환 1분 요건의 의도적 역정규화). 로컬 재임베딩 1,051청크 GPU 38초. **판정 API 1차** `POST /npc/judge` — Gemini Flash-Lite / EXAONE 7.8B(Ollama) 이중 어댑터, 안전장치 3종. A/B 1차에서 제재 주체 오인 케이스에 Gemini 오판정·EXAONE 정답 확인.
- **v0.8.0** — 행정규칙(admrul)·법령 별표 수집: 2026년 최저임금 고시(시간급 10,320원), 단순노무직종 지정 고시(수습 감액 함정 근거), 시행령 별표 7종. 첨부 PDF 본문 추출 폴백.
- **v0.9.0** — 고용노동부 수동 수집 문서 ingestion: 근로기준법 질의회시집 회시 399건 + 통상임금 노사지도 지침. **총 1,593청크 × 2개 벡터 공간(Gemini/로컬) 완성.**
- **실현가능성 검증 문서** 작성: "이미 돌아가고 수치로 측정했다" 원칙으로 전 파이프라인 실측 기록(RTX 5060 Ti 16GB). 계획 대비 W1 완료 + W2 조기 달성.
- **AI 모델 구성 v1 확정**: 온라인 Gemini / 오프라인 Qwen3-Embedding-4B + EXAONE 7.8B, 대사 생성만 외부 API(VRAM 16GB 예산).

## 2026-08-04 — ERD v1.0

- 서비스 4개 축(메인게임·미니게임·가드·리포트)을 테이블 구조로 번역한 **ERD v1.0** 확정. 설계 규칙: 1NF→3NF 정규화, 고립 테이블 금지, 1 테이블 = 1 Fractal 11-File Set.
- 기획서 보강(`4개층원리.md`, 주요특징·핵심기술).

## 2026-08-23 — 프론트엔드 집중 개발일: v0.1.0 → v0.6.3 하루 완주

FE-brief 확정(웹 데스크톱 퍼스트, Phaser는 허브맵만·나머지는 React DOM — 한글 IME 안전 설계)과 함께 프론트 스캐폴딩부터 게임 화면까지 하루에 구축.

- **v0.1.0 — 스캐폴딩**: Next.js + React 19 + TS strict + Tailwind 4. Phaser 4 안전 통합(EventBus 싱글턴, StrictMode 이중 마운트 가드), feature-sliced 디렉토리 골격, 포트 3100 고정, 하드코딩 localhost 0건.
- **v0.1.1 — Next.js 16.3.2 업그레이드**: Phaser 4.2.1 + Turbopack 조합 검증 통과, npm audit 0건.
- **v0.2.0 — 폰트·테마**: Galmuri11(픽셀) + Pretendard(법령 조문) self-host — 외부 CDN 참조 0건(폐쇄망 시연 요건). 게임 톤/유틸리티 톤 CSS 변수 체계.
- **v0.3.0 — API 계약 v1 + MSW 목 서버**: 엔드포인트 5종·이벤트 17종 계약(계약 우선 방식, 백엔드 구현 스펙의 원본). 키워드 판정기 + 실제 조문 원문(법제처 API 현행 기준) 목 데이터. `npm run check:contract` 10스텝 자동 검증 PASS.
- **v0.4.0 ~ v0.4.1 — 대화 방어전 화면**: useBattle 상태 머신(세션 멱등 시작·체크포인트 재개·판정 루프·방어선 돌파), **한글 IME 조합 중 Enter 가드**, 방어선 게이지 3단, 타이프라이터 대사, 근거 카드(조문 원문 렌더링). 팀장 지시로 웹 퍼스트 원칙 명문화 + 풀스크린 스테이지 레이아웃 재구성.
- **v0.5.0 — 허브맵(Phaser 4, D-37)**: ASCII 배열 맵 + 격자 이동(방향키/WASD)·벽 충돌·노드 상호작용. 진행 상태 SSOT는 zustand, 씬은 상태 미소유(`map:hydrate` 주입). EventBus 이중 인스턴스 번들 사고·StrictMode 리스너 전역 삭제 사고 두 건 해결.
- **v0.6.0 — 결과 화면**: 스탯 변동 카드 + "지켜낸 돈" 카운트업 연출, 체크포인트 localStorage 영속(새로고침 복귀), 클리어 시 다음 퀘스트 언락. check:contract 11스텝 PASS.
- **v0.6.1 ~ v0.6.3 — 실제 스프라이트 파이프라인**: 3면도 시트 크롭 → 마젠타 키잉 → 캔버스 정규화 → 48×48 네이티브 픽셀 퍼펙트 세트 교체. 허브맵 격자를 48px 기준(타일 32px, 맵 36×16 확장)으로 전환. 캐릭터 스프라이트 제작 규격 가이드 문서화.

## 2026-08-24 — 풀스택 관통일: 인증 → 게임 API 이관 → 2-Call 판정 → Guard OCR (BE v0.10.0→v0.12.15 / FE v0.7.0→v0.8.17)

하루에 백엔드 19개 버전·프론트 27개 버전. 설계서 승인(superpowers 설계 문서 7종 + 구현 플랜 4종 — auth, gameplay-backend, dialogue-2call, guard-ocr, quest-replay-score, play-records-reset, stat-view-dropdown) → TDD 구현 → 실연동 검증 사이클을 네 축(인증·게임 플레이·판정·가드)에 반복 적용.

**① 인증 풀스택 (BE v0.10.0~v0.10.1 / FE v0.7.0~v0.7.2)**

- **BE v0.10.0 — auth BC**: 아이디·생년월일·비밀번호 가입/로그인, 구글 OAuth(생년월일 1회 온보딩), access JWT(15분) + refresh HttpOnly 쿠키(7일, 매 갱신 rotate). 테이블 3종(`player`/`player_oauth`/`refresh_token`, Alembic), API 8종(`/api/v1/auth/*`), CORS localhost:3100 + credentials, **테스트 15건**. DB에는 `birth_year`(연도)만 저장.
- **BE v0.10.1** — 구글 OAuth 실연동 준비: Keymaker `GOOGLE_*`/`FRONTEND_ORIGIN` 시크릿, CSRF `lt_oauth_state` 쿠키, 미설정 시 `GOOGLE_DENIED` 안전 폴백.
- **FE v0.7.0 — auth feature**: 로그인/회원가입/생년월일 온보딩/구글 버튼 UI, `AuthGate`로 게임 라우트 보호, `authStore`(accessToken 메모리 only — persist 금지) + `apiFetch` 401→refresh 자동 재시도, MSW auth 핸들러.
- **FE v0.7.1~v0.7.2** — 허브맵 헤더 로그아웃 버튼, auth만 실백엔드 bypass하는 하이브리드 모드(과도기 구성).

**② 게임 플레이 API 백엔드 이관 (BE v0.11.0~v0.12.2 / FE v0.7.3, v0.8.0~v0.8.4)**

- **BE v0.11.0 — quest BC gameplay API**: `GET /quests` · `POST /quest-sessions` · `POST /dialogue-turns` · `POST /hints` · `POST /learning-events` 5종, 테이블 7종(`category`/`chapter`/`quest`/`defense_line`/`quest_session`/`player_stat`/`learning_event`, Alembic), Q-ALBA 시드, `JUDGE_PROVIDER=fake|gemini|exaone` Factory.
- **BE v0.12.0** — `quest_clear_score` 장부: 퀘스트별 **최초/최근 보상 분리**, CLEAR 시 `player_stat` 재합산, 턴 결과 `scoreBucket`.
- **BE v0.12.1~v0.12.2** — 연습 모드(CLEARED 퀘스트 즉시 재도전, `PRACTICE`면 점수·money·학습 무기록) + `DELETE /play-records`(계정 유지 초기화).
- **FE v0.7.3** — `FALLBACK_MODE=0`에서 MSW 완전 비활성 — auth·게임 API 전부 실백엔드(8100). **MSW 목 시대 종료.**
- **FE v0.8.0~v0.8.4** — 스탯 카드 최초/최근 드롭다운 전환, 연습 모드 안내 문구, 허브 헤더 「플레이 기록 제거」 버튼.

**③ 2-Call 판정 아키텍처 (BE v0.12.3~v0.12.8)** — "판정은 로컬 SLM, 말투만 외부 API"

- 판정 `JUDGE_PROVIDER=exaone`(로컬 SLM + pgvector RAG) / 대사 `NPC_SPEECH_PROVIDER=gemini|seed` 분리 — `NpcSpeechPort` + Gemini/Seed 어댑터.
- 유저 발화 벡터 검색 → `articles[]`를 판정 프롬프트에 주입. 방어선 CLEAR 근거 조문은 `RagLawEvidenceAdapter`가 **DB 원문**으로 반환(RAG 실패 시에만 시드 폴백) — 할루시네이션 방지 설계 유지.
- SLM 미기동·모델 없음 시 키워드 판정 폴백, `OLLAMA_BASE_URL`로 원격 Ollama 지원.
- **E2E 실측**: EXAONE 7.8B + RAG 판정이 법령 근거와 함께 CLEAR 반환 — warm 추론 1.4~2.0초, 전체 왕복 3.05초(GPU 100% 로드).

**④ Guard OCR 계약서 진단 (BE v0.12.9~v0.12.15 / FE v0.8.13~v0.8.17)** — 목업이던 가드를 실기능으로

- BE: `ContractScanInteractor` 제출→OCR→필드 확인→판정→리포트→파기 전체 흐름(TDD), PaddleOCR 어댑터(`grid_ocr_manager` 싱글턴, zonal crop + 임금 휴리스틱, `GUARD_OCR_PROVIDER=fake|paddle`), 리포트에 법령 인용·상담기관 연계·퀘스트 추천 시드 어댑터 3종 부착.
- 개인정보 설계: OCR 완료·실패(`UNREADABLE`) 모두 **원본 이미지 즉시 삭제** + `image_purged_at` 기록, 파기 시 `PURGED` 상태 전이 후에도 finding 메타데이터 기반 리포트 조회 유지.
- 데이터 정합: 2026년 최저임금 기준을 **시간급 10,320원**으로 정정(guard_rule 데이터 마이그레이션 포함), 문자열 `"true"`/`"1"` 불리언 판정 버그 수정, 근로기준법 제20조 인용을 검증된 단일 조문으로 축소.
- FE: `/guard` 전체 플로우 `entry → capture → analyzing → confirm → report` 완성 — 촬영 품질 게이트(10MB·MIME·라플라시안 흐림 검사), EXIF 제거·개인정보 가림 확인 절차, 저신뢰 필드 직접 입력·PATCH 확정, 심각도·조문 원문·상담 CTA·추천 퀘스트 리포트, `UNREADABLE` 재촬영 UX(회귀 테스트 포함).

**⑤ 허브맵 UX·원격 협업 (FE v0.7.4~v0.7.8, v0.8.5~v0.8.12)**

- 실백엔드 전환 후 드러난 하이드레이션 버그 연쇄 해결: 새로고침 시 노드 색 유실·퀘스트 진입 불가·StrictMode 세션 시작 스킵 — 최종적으로 EventBus 레이스를 폐기하고 `PhaserHost(nodeStates)`가 씬의 `hydrateNodes()`를 직접 호출하는 구조로 확정.
- 허브맵을 세로 필드(도로·잔디·나무·물·편의점 노드)로 교체, 이동키 1회에 다음 노드까지 연속 이동, 카메라 추적, CLEARED 재진입은 맵 노드 상호작용으로 일원화(중복 버튼 제거).
- **FE v0.8.12 — Next.js 리버스 프록시**(`/api/*` → `BACKEND_ORIGIN`): Tailscale/LAN 원격 기기에서 로그인 실패하던 문제 해결 — 프론트 담당 신채연 원격 작업 환경 개통, 데모 계정 발급·검증.
- 다음 단계: 미니게임(CALC/DOC), 구글 OAuth 실연동, PaddleOCR 실촬영 이미지 검증, 신규 캐릭터 에셋 파이프라인.

## 2026-08-25 — 게임 월드 대확장일: CALC 던전 풀스택 + 허브 오픈월드·편의점 실내·전투 UI 리뉴얼 (BE v0.12.16→v0.12.23 / FE v0.9.0→v0.20.27)

하루에 백엔드 8개 버전·프론트 **92개 버전**. 백엔드는 Stage 2 주휴수당 CALC 던전과 판정 평가 하네스를 닫았고, 프론트는 허브맵을 Modern Exteriors 기반 가로 거리 오픈월드로 갈아엎은 뒤 실내 씬·NPC 시나리오·포탈·전투 UI까지 한 번에 밀어붙였다. 설계 문서 4종(stage2-holiday-pay, w3-remaining, judge-eval-stabilization, wiring-probe-myself) + 구현 플랜 2종 승인 → TDD 사이클 유지.

**① Stage 2 주휴수당 CALC 던전 풀스택 (BE v0.12.16~v0.12.19 / FE v0.11.0)**

- **BE v0.12.17** — `calc_problem` 테이블(Alembic, A5 Q1~Q5 시드) + `holiday_pay` 도메인 서비스(`expected_answer(kind, params, wage)` 정답 계산), quest BC 전용 `MIN_WAGE_2026 = 10,320원` VO. Q-ALBA-002 지켜낸 돈을 Q5 총액 **330,240원** 런타임 계산으로 정정.
- **BE v0.12.18~v0.12.19** — `quest_session.calc_progress` JSONB로 문항 조회·정수 답안 제출·시도 횟수 추적, `start_or_resume` CALC 세션 지원, **최종 정답 4개 이상 시 CLEAR**(기존 점수 장부 재사용). HTTP 스키마·라우트 + FastAPI 의존성 오버라이드 계약 테스트.
- **BE v0.12.16** — `GEMINI_API_KEY` 부재 시 `POST /quest-sessions` 500 버그 수정(`KeymakerSecretError`를 미지 프로바이더로 오인) — 로컬 기본 임베딩 `local`로 키 없이 세션 시작 가능.
- **FE v0.11.0** — `/minigame/holiday-pay` feature slice: 서버 제공 문항·시급 기반 규칙 카드, 5문항 진행, 숫자 키패드, 힌트·해설, CALC 클리어 → 기존 결과 화면 연계.

**② W3 대화 마감·판정 평가 하네스·전 BC 배선 프로브 (BE v0.12.20~v0.12.23)**

- **v0.12.20 — W3 잔여**: `dialogue_turn` 테이블 + 저장 전 **PII 정규식 스크러빙**(`scrub_pii`), 동일 방어선 3턴 이상 시 ONGOING에도 `hint1` 자동 부착, Fake A4 E2E + 오프라인 스모크 스크립트(P0-4).
- **v0.12.21 — 판정 평가셋 안정화**: A2 §4 평가셋 시드 + `run_judge_eval.py`(기본 Fake, gemini/exaone 실측 선택), 오입력 10케이스 추가. 실측 리포트(`docs/judge_eval_report.md`): **A2 25케이스 정확도 100%, 오입력 10케이스 오CLEAR 0**. Fake↔실LLM 전환 문서화 — `.env` 1~3줄, 1분 이내 전환(P0-4·W5).
- **v0.12.22** — 로컬 Guard OCR을 `GUARD_OCR_PROVIDER=paddle`로 전환 — PaddleOCR 실문서 필드 추출 동작.
- **v0.12.23 — myself 배선 프로브**: `wiring_probe` 테이블 + `grid_wiring_probe`(콘솔+DB 기록), **전 6개 BC**(ontology/quest/npc/auth/game/guard) myself 엔드포인트에 `probeId`·`layers` 부착, 6경로 × 3레이어 자동 검증 테스트.

**③ 허브맵 오픈월드 리뉴얼 (FE v0.9.0~v0.17.7)**

- **v0.9.0** — 세로 노드 레일 맵 → **가로 거리 오픈월드**(Modern Exteriors 48×48 에셋 33종 큐레이션, 유료 라이선스라 gitignore). 상단 상가 / 도로 / 하단 인도·공원, 소품 배치 `chapter1-props.ts` 콘텐츠 분리, y기준 depth 정렬. 충돌은 화이트리스트 → 블랙리스트로 전환해 자유 이동.
- **v0.10.0·v0.12.0·v0.13.0** — **클릭/터치 A\* 길찾기**(easystarjs), **8방향 대각선 이동**(코너 컷 금지 + 슬라이딩, √2 등속 보정), 대각선 전용 스프라이트 4종(NE/NW/SE/SW), 캐릭터 표시 높이 48→**72px**.
- **v0.14.0~v0.16.3 — 도로 생태계**: 횡단보도(맵 문자 `+`), 차량 4대 순환 주행(연두 버스·주황 택시·검정 해치백·흰 SUV — 팩에 없는 색은 HSV 리컬러), 차량↔플레이어 상호 충돌, **차간 정지 로직**(앞 차 간격 이내 정지 → 2.5초 후 재주행).
- **v0.17.0~v0.17.7** — Phaser 스케일 `FIT`→`RESIZE`(레터박스 제거, 1:1 픽셀), 맵 60×21→**45×21** 압축, 건물 3동을 콘도·중형 마켓으로 교체(`HUB_STORES` 메타 객체화 — 건물별 폭·문 위치 콘텐츠 지정).

**④ 편의점 실내 씬 + 백엔드 무의존 목 서버 (FE v0.18.0~v0.18.19)**

- **v0.18.0 — `InteriorScene`**: 1번 가게 문 진입 시 top down 편의점 실내로 전환(Grocery Store 팩 큐레이션, `store1-interior.ts` 콘텐츠 분리). NPC 접촉/클릭 → 퀘스트 진입, 출구 매트 → 허브 문 앞 복귀.
- **v0.18.3 — `npm run dev:mock`**: 백엔드 다운 상태에서도 도는 목 dev 서버(포트 3101). Next 16의 distDir당 1서버 잠금을 `NEXT_DIST_DIR=.next-mock`으로 우회해 3100과 병행, MSW에 demo 계정 시드 + 목 refresh 세션 복원.
- **v0.18.2~v0.18.19** — 실내 디테일 16회 반복 다듬기: L자 카운터 180도 회전(카운터 너머 대화), 진열대·쿨러·ATM·핫푸드 워머 재배치, 퀘스트 배경과 동일한 크림+민트 스트라이프 벽 합성, 벽 매립 뒷문.

**⑤ 월드 디테일·NPC 배치 (FE v0.19.0~v0.19.23)**

- **v0.19.0 — 건물 사이 백드롭 4구간**(`chapter1-backdrop.ts` + 에셋 17종): 잔디밭+빌보드, 뒷골목, 필러 콘도 2동, 차기 챕터용 공사장.
- **v0.19.1~v0.19.4** — 나사장 NPC를 임시 네모 → **pixellab 생성 스프라이트**로 교체(몸 폭 기준 ×2 정수 배율 보정), 플레이어 걷기 바운스 애니메이션.
- **v0.19.5~v0.19.13** — **세로 차도** 신설(맵 문자 `V`, 반 칸 확폭·보행 전환) + 가로 횡단보도(`x`), 건물 존 전면 인도 타일화, LIME CORP 오피스 빌딩·버스정류장(합성 에셋) 배치.
- **v0.19.14~v0.19.23** — 공원 분수·꽃덤불·실내 ATM, **앉아있는 친구 NPC**(pixellab 생성, 머리 크기 기준 배율), 퀘스트 상태 말풍선 2종: 클리어 시 친구 머리 위 **물방울**, 미클리어 시 나사장 머리 위 **빨간 느낌표**(시트 픽셀 실측 크롭, hydrateNodes 상태 연동).

**⑥ 시나리오·포탈·전투 UI 리뉴얼 (FE v0.20.0~v0.20.27)**

- **시나리오 대화 (v0.20.0·v0.20.3·v0.20.11~v0.20.12)**: 주휴수당 미니게임 진입을 공원 친구 대화로 이동(EventBus 5번째 이벤트 `map:friend-selected`), 나사장 접근 인사 5줄 시나리오(`content/dialogs.ts` 분리) 후 '반박하러 가기!' → 전투. 퀘스트 종료 시 실내 원래 자리로 복귀(`returnPoint`, 1회 소비).
- **포탈 (v0.20.4~v0.20.5·v0.20.20·v0.20.22~v0.20.26)**: 노드 1을 마커 → 골드 포탈로, **밟으면 자동 입장 + 250ms 페이드 전환**, 메이플풍 **빛기둥 연출**(프로시저럴 픽셀아트 스프라이트시트 자체 제작, 48×96 8프레임 — 형태 3회 반복 조정으로 낮고 넓은 빛 커튼 확정). 키 홀드로 포탈을 지나치던 입력 버그 수정.
- **전투 화면 리뉴얼 (v0.20.6~v0.20.10·v0.20.13~v0.20.19·v0.20.21)**: refs/quest-example.png 레이아웃(전체 화면 편의점 배경 + 배너/이름표/스탯/대사창/행동 메뉴)으로 재구성 후 **기존 게임 톤(다크 서피스+적대 레드)으로 재통일**. 컨테이너 1440px, 하단 3패널 높이 통일 + 입력창 2행 분리, 행동 메뉴 ▶ 포커스, 화자 인라인 표기, 시나리오·전투 대사창 타자 효과 통일, "반박 성공!" 중앙 bounce 배너.
- **대화 중 이동 차단 (v0.20.21)** — EventBus 6번째(상한) 이벤트 `map:dialog`: 대화 열림 시 씬 키보드 비활성·경로 큐 초기화, 닫힘 시 복원.
- **v0.20.27 — 횡단보도 신호등 시스템**: 가로도로 횡단보도 2곳에 신호등 자동 배치(맵 `+`에서 유도), 플레이어 접근 시 빨강→초록·차량 정지, 횡단 완료/이탈 시 복귀. 걸친 차는 최소 이동 후 스냅 정지.
- 다음 단계: DOC 미니게임, CALC 던전 실백엔드 연동 검증, 전투 캐릭터·초상화 일러스트 입고, 구글 OAuth 실연동.

## 2026-08-26 — 폴리시·연출의 날: 신호등 교통 완성 + 전투 연출·수호천사 펫 + Guard DOCX + 랜딩 라이브 허브맵 (BE v0.12.24→v0.12.27 / FE v0.20.28→v0.20.52)

백엔드 4개 버전·프론트 25개 버전. 어제 열어둔 월드를 하루 종일 다듬는 날 — 신호등 교통 체계를 실제 교통처럼 완성하고, 전투 화면에 나사장 스프라이트·수호천사 펫·반박 성공 연출을 얹고, 가드에 Word(.docx) 업로드를 풀스택으로 열고, 랜딩 페이지를 정적 히어로에서 **살아있는 허브맵 어트랙트 모드**로 교체했다. 심사 가독성 대비 폰트 전면 확대와 MSW/실백엔드 세션 안정화까지. 별도로 insight BC(상담 준비 리포트) 설계 문서 v2.2 확정.

**① 신호등 교통 체계 완성 (FE v0.20.28~v0.20.33)**

- 차량 반응을 **횡단보도별 독립 동작**으로 변경: 빨간불 쪽 차는 평소 주행, 초록불에 걸친 차는 **2.2배 가속 탈출** 후 정상 복귀, 접근 차는 정지선(가장자리 ±8px)에서 정지 — 뒤차는 tailgating으로 자연스럽게 줄을 선다.
- 차량 겹침 방지 하드 클램프(`clampBehindLeader`, 앞 차 꽁무니 GAP 24px), 신호등 기둥 충돌 처리(키보드·A* 길찾기 모두), 초록불 트리거 범위 확장(대기 구역 좌우 1칸 + 무단횡단 지브라 진입 반응).
- 포탈 광선 하단의 흰 직사각형 아티팩트 제거(심지 막대 삭제 + 알파 페이드) 후 타원형 발광 코어로 "바닥이 빛나는 느낌" 복원. 인도 타일은 회녹색 사선 격자(Others_7)로 최종 교체.

**② 편의점 실내 비주얼 완성 (FE v0.20.34·v0.20.36~v0.20.37·v0.20.42)**

- 바닥을 quest-bg 웜 베이지(216,197,181) 리컬러 타일로, 벽은 `Room_Builder_3d_walls` 키트를 quest-bg 팔레트(크림+청록 스트라이프)로 재색칠해 뒷벽·측벽·코너를 단일 키트로 재구성 — 코너 접합 열 추가로 캡→사선→벽면 연속 접합.
- 최종적으로 **사전 렌더 벽 링 한 장**(`wall-ring.png` 768×576, 재생성 스크립트 `gen-wall-ring.py`)으로 합성: 뒷벽면 3타일 확장, 민트 띠가 모서리를 감아 도는 연출, 남쪽은 바닥+흰 캡 스트립 확정, 충돌선을 벽면 바닥 경계(y144)와 정합. 중앙 집기 반 칸 하향·벽 간판 4종 제거.

**③ 전투 화면 연출·수호천사 펫 (FE v0.20.38~v0.20.42·v0.20.50)**

- **나사장 전투 스프라이트**(128×160, PixelLab Pro — 맵 48×48과 동일 외양의 고디테일 정면 팔짱 포즈)를 배경 위 실내 NPC 위치 대응 지점에 배치.
- **수호천사 펫 확정: 천사 강아지**(크림 강아지+갈색 귀+금색 후광+흰 날개, PixelLab Pixflux). 허브 동행판은 품질 이슈로 철회하고 **전투 화면 전용**으로 확정 — 스탯 패널 위 둥둥 부유(`pet-float`), 힌트를 펫 말풍선(CSS 픽셀아트, 이미지 불사용)으로 이동, 말투 변환 `petify`('멍' 부착, 따옴표 안 모범답안은 원문 보존).
- **반박 성공 시퀀스**: 나사장 피격 흔들림 + '반박 성공!' 배지 → 클리어 대사 타자 효과 → **대사창 클릭 시에만 조문 카드 공개**. 방어선 게이지는 HP 스타일로 반전(돌파마다 깎임). 결과 화면 패배 초상화 자리에 펫 배치.
- 대화→전투 250ms 페이드 전환, PLAYER 스탯 3종(신용·멘탈·법률지식) 게이지화, **전투·허브·던전 폰트 전면 확대**(심사 가독성, 해커톤 대비).

**④ 허브 상태 연출·스몰토크 UX (FE v0.20.43~v0.20.45·v0.20.47~v0.20.49)**

- 노드 2·3 숫자 마커 → **상태 반응형 포탈**: LOCKED 회색 정지 / AVAILABLE·IN_PROGRESS 금색 맥동+빛기둥 / CLEARED 파란 맥동 — 퀘스트 1 클리어 시 노드 2가 해금되며 회색→금색 전환.
- hydrate 버그 2건 해결: 클리어 후에도 빨간 느낌표 잔존(`node: null` 조건 버그), 실내→허브 복귀 시 친구 물방울 누락(CREATING 씬 hydrate 스킵). 친구 말풍선은 노란 물음표로 교체.
- **대화 중 이동 차단 실효화**(`dialogOpen` 게이트 — `key.isDown`·클릭 길찾기 모두 차단), NPC 대화 **Space 진행**(스킵/다음 줄/마지막 줄 전투·미니게임 진입).

**⑤ 세션·진행 초기화 안정화 (BE v0.12.24~v0.12.25 / FE v0.20.35·v0.20.43·v0.20.46)**

- **MSW 하드 리로드 로그인 튕김 근본 해결**: StrictMode 이중 이펙트에서 `worker.start()` 거부 경로가 게이트를 조기 개방 → 핸드셰이크 전 refresh가 실백엔드 401. 센티널(`GET /msw-ready`) 확인 후 렌더 + 목 auth 마지막 로그인 영속(`lt-mock-auth-v1`)으로 수정.
- **실계정 로그인 실패 해결**: `NEXT_PUBLIC_FALLBACK_MODE=0`(실백엔드) 전환 + 잔존 mockServiceWorker 등록 해제.
- **BE v0.12.24 — 플레이 기록 전면 초기화**: `DELETE /play-records`가 대화 로그→세션→점수→학습 이벤트→스탯에 더해 **가드 스캔·이미지·finding까지** 삭제(`ProgressWipePort` + `GuardProgressWipeAdapter`). 허브 `moneyDefendedTotal`은 CLEARED 집합(세션∪점수장부) 기준 합산으로 정정.
- **BE v0.12.25 — EXAONE 판정**: `keep_alive` 30m만 유지, `num_predict` 상한·RAG 조문 360자 절단은 철회(전문·무제한 출력 복원).

**⑥ Guard DOCX 업로드 풀스택 + OCR 정확도 (BE v0.12.26~v0.12.27 / FE v0.20.51)**

- **BE v0.12.26** — 텍스트 Word(`.docx`) 본문·표 추출(`python-docx`) 후 기존 필드 휴리스틱 재사용. ZIP+`word/document.xml` 매직·MIME 검증, 스캔 이미지 DOCX·구형 `.doc` 제외 (설계: `2026-08-26-guard-docx-design.md`).
- **BE v0.12.27** — 근로계약서 OCR 정확도: `LABOR_CONTRACT`는 zonal crop을 건너뛰고 전문 텍스트에서 임금·소정근로·주휴일·연차·위약금 추출, `시간급`/`시급`만 시급 인정(1,000~100,000원 범위) — 표준연소근로계약서의 `wage=2`·연차 `14` 오인식 수정.
- **FE v0.20.51** — 가드 업로드에 JPG/PNG + `.docx` 선택 지원. 블러·EXIF는 이미지에만, DOCX는 파일명 미리보기·본문 텍스트 점검.

**⑦ 랜딩 페이지 리브랜딩 + 전체화면 모드 (FE v0.20.50·v0.20.52)**

- 랜딩을 정적 히어로 한 장 → **라이브 허브맵 어트랙트 모드**로 교체: 실제 허브 씬을 캐릭터 숨김·입력 차단으로 마운트(차량·신호등 동작) + 반투명 네이비 오버레이 + 로고 + 로그인/PLAY DOM 버튼. PLAY 클릭 시 400ms 암전 후 `/hub`의 카메라 fadeIn과 이어지는 심리스 전환.
- 브랜드 에셋: 나노바나나 로고(금색 아웃라인판, 알파 트림 1068×575)·버튼 시트(`goodbuttons.png` 분리 크롭, 512px LANCZOS 리샘플) 교체 — 경로 유지로 코드 변경 없음.
- **전체화면 모드**(`shared/ui/FullscreenButton`, Fullscreen API): 라우트 이동 후에도 유지, ESC 해제 안내 4초 표시. 가드 버튼은 허브 헤더로 이동.

**⑧ insight BC 설계 문서 (docs/상담리포트.md v2.2)**

- 상담 준비 리포트 문서 생성기 스펙 확정 — 학생의 두루뭉술한 진술을 전문가(노무사·변호사·전담기관) 양식으로 **형식만 번역하고 해상도는 올리지 않는** 원칙, 수치 팩트 시트 **값/없음/모름 3상태** + 재권유 루프. 선행 산출물(법령 RAG 1,593청크·OCR 파이프라인·가드 rule_code 19종) 재사용 설계.
- 다음 단계: 프론트 변경분(v0.20.52) 커밋(frontend만 스테이징), insight BC 구현 착수, DOC 미니게임, 구글 OAuth 실연동.

---

## 2026-08-27 — insight 풀스택 관통 + AI 신뢰성 평가의 날: 상담 준비 리포트 설계→구현→146케이스 평가→gemma 확정, 방어전 75발화 배터리 오CLEAR 0, VRAM 배분 결정 (BE v0.12.28→v0.15.11 / FE v0.21.0→v0.27.10)

하루에 백엔드 29개 버전·프론트 49개 버전. 어제 설계 문서(v2.2)를 확정한 insight BC(상담 준비 리포트)를 **백엔드 구현 → 프론트 → 신뢰성 평가셋 146케이스 → 3모델 교차 평가 → gemma3:12b 확정 → 실백엔드 전환 → 30일 보관 정책**까지 하루에 관통했다. 방어전은 상호 대응 대사 시스템을 얹고 75발화 자연어 배터리로 신뢰성을 검증했으며(오CLEAR 0), 가드는 위반 검출 시나리오 VIOL-01~05를 닫았다. 자원 측면에서는 임베딩을 gemini로 내보내 VRAM 16GB를 LLM 전용으로 배분하는 의사결정을 확정. superpowers 설계·플랜 6종(insight-backend, insight-eval-set, insight-report-frontend) 승인 → TDD 사이클 유지.

**① insight BC 백엔드 — 상담 준비 리포트 (BE v0.13.0~v0.13.1)**

- `/api/v1/insight/report/sessions` 4+1 엔드포인트(start/answer/reask/preview/generate), **유형 불가지 엔진 4모듈**(classifier·interviewer·extractor·reporter), 팩·세션 DB 4테이블 + WAGE_THEFT 15슬롯·12유형 taxonomy 시드(Alembic).
- LLM은 `INSIGHT_LLM_PROVIDER=fake|gemini|exaone` 3-way Factory. **추출 안전장치**: raw_quote 부분 문자열 검증 실패 시 UNKNOWN 강등·재시도 1회. 조문은 ontology RAG ACL 재사용(DB 원문, fail-open). generate 직후 진술 데이터 즉시 파기(purged_at 증빙). 테스트 30여 개(엔진·라우터·금지어·계산 부재 게이트).
- v0.13.1 — LLM transport 장애 전용 예외(`InsightLlmUnavailableError` → 502) 분리, 재권유 문구를 코드 상수에서 팩 데이터로 이관("엔진은 유형을 모른다" 원칙 정합).

**② 상담 준비 리포트 프론트 (FE v0.21.0→v0.23.0→v0.27.0→v0.27.10)**

- v0.21.0 허브 헤더 상담리포트 버튼("준비 중" notice) → v0.23.0 `/report` feature 슬라이스 신설: 자유서술 → 채팅 인터뷰(메신저형 말풍선, 모름/없음 구분·재권유 1회) → 팩트 시트 확인(본인 확인 체크 게이트, ✎ 슬롯별 수정) → 8섹션 리포트+학생용 별지+브라우저 인쇄. 백엔드 미구현 시점이라 인프로세스 페이크가 서버 역할 — API 계약은 스펙 §9 그대로 두어 전환 시 한 파일만 교체하는 설계.
- v0.24.2 최종 검수 — 날짜 아닌 슬롯에 "…경" 접미가 붙던 오표기("카페경") 수정, 수집하지 않은 진술 조작 표기를 `[미확인]`으로 정정, **"전문가 검수 진행 중" 배지 제거**(실제 검수가 없는데 오해 소지).
- v0.27.0 **실백엔드 전환** — 페이크 삭제, answer 구조형 `{kind: TEXT|DONT_KNOW|NONE}`, 서버 15슬롯 전체 소화. 실백엔드 E2E 3시나리오(해피패스·전세 fallback·보이스피싱 긴급) 콘솔 에러 0 통과. v0.27.9 가드 진단 결과 → 상담리포트 연결 버튼.

**③ insight 신뢰성 평가셋·하네스 146케이스 (BE v0.13.3~v0.14.2)**

- 라벨 발화 138건(분류 68 + 추출 70) + E2E 페르소나 8건. 분류는 WAGE_THEFT 변형(표준·오타·은어·간접) 20 + 11유형×3 + 복합 10 + 모호 SOS 5, 추출은 핵심 10슬롯 × EXACT/APPROX/HEARSAY/UNKNOWN 4등급 + **TRAP 10건**(값을 암시만 하는 발화 — 정답은 전부 NULL, 각색 감지 안전장치) + MULTI(한 문장 다슬롯) 10건. 페르소나는 STANDARD·CHATTY·TERSE·DONT_KNOW_HEAVY·ALL_NONE·EDIT_HEAVY·URGENT_EXIT·FALLBACK_RESTART 8종.
- 러너 `run_insight_eval.py`: 분류 정확도·추출 값/precision 일치·**날조 치명 게이트**(발화·허용 소스에 없는 숫자·신규 한글 토큰이 나오면 fatal)·페르소나 완주를 마크다운 리포트로. 합격 기준: **치명 0 필수 + 분류·값 일치 ≥90% + precision ≥80%**. fake 축소판은 pytest 상시 회귀로 편입.
- v0.14.2 채점 공정화 — 날조(치명)/계약위반(비치명) 분리, 숫자 앵커 폴백("5명" vs "5" 표기 차이 구제), 프로바이더 카나리아 프리플라이트 등 4건 수정 후 재실행: 스코어러 결함과 실제 모델 성능 한계를 분리해 확인.

**④ 3모델 교차 평가 → gemma3:12b 확정 (BE v0.15.0·v0.15.1·v0.15.4)**

- **exaone3.5:7.8b**(로컬): 프롬프트 보강 3차례에도 추출 값 77.4→82.1→83.3%·precision ~70%에서 정체 — 7.8B 성능 한계로 판정.
- **gemini-2.5-flash-lite**(온라인): 분류 100%·값 89.3%·precision 90.5%·날조 0 (JSON Schema union 호환 수정 후 첫 유효 실행).
- **gemma3:12b**(로컬, 어댑터는 모델명 오버라이드 14줄): **값 92.9%·precision 91.7%·분류 98.5%·날조 0 — 오프라인 모델 최초로 전 통계 기준 충족.** 분류 프롬프트에 규칙 2줄(긴급 피해 우선·직장 내 관계 한정) 추가로 **분류 100%(68/68)·urgent 누락 0** 달성 → 상담리포트 서빙 모델 gemma 확정.

**⑤ 모델 서빙 전략 — VRAM 16GB를 어디에 쓸 것인가: 임베딩 gemini 전환 (BE v0.15.4·v0.15.6·v0.15.7)**

오늘의 가장 중요한 자원 배분 의사결정. GPU에 올라가고 싶은 모델이 셋(임베딩 Qwen3-4B ~8GB·판정 exaone 7.8B·리포트 gemma3:12b ~8.7GB)인데 VRAM은 16GB 한 장 — 웜업 순서가 어긋나면 `torch.OutOfMemoryError`가 재현되고, 임베딩이 10.7GB를 선점하면 gemma가 부분 오프로딩으로 ~3초까지 늘어지는 것을 실측으로 확인한 상태였다.

- **판단 기준: 실측 성능 차이가 큰 곳에 희소 자원을 준다.** LLM 쪽은 차이가 컸고(exaone 정체 vs gemma 전 기준 통과 — ④), 임베딩 쪽은 차이가 없었다(MTEB 기준 Qwen3-4B와 gemini-embedding-001 동급 + 스키마가 애초에 1536차원 통일 설계). 게다가 본선 배포가 **Vercel 프론트 + Cloudflare 터널 백엔드**로 확정되며 인터넷이 시연의 전제조건이 됨 — "폐쇄망 대비 로컬 임베딩" 논리 자체가 소멸했다.
- **결정: `EMBEDDING_PROVIDER=gemini`, VRAM은 LLM 전용 (v0.15.6).** 스위치 한 줄 전환 후 스모크: "임금 체불" 질의 **0.7초**에 정확한 조문 반환. 효과는 즉시 — gemma 오프로딩 없이 완전 상주(8.0/8.0GB), 나아가 **exaone(5.2GB)+gemma(8.0GB) 동시 상주 실측 성공(총 14.0/16.3GB, 상호 축출 없음, v0.15.7)**. 웜업 런북을 4단계(RAG 프리플라이트→exaone→gemma→공존 검증)로 확정하며 "리포트 전환점 모델 스왑 흡수" 설계 자체가 불필요해졌다 — 시연 전 구간 웜 상태. 프리워밍 엔드포인트(`/report/warmup` + 프론트 intro 마운트 fire-and-forget, v0.15.4)로 첫 제출 대기 ~15s→~3s.
- **되돌릴 수 있는 결정**: `law_chunks` 1,593건에 gemini/local 벡터가 컬럼으로 공존(설계 시점의 의도적 역정규화) — 복귀는 재임베딩 없이 provider 스위치 1분. 로컬 임베딩을 "안 해본" 게 아니라 어댑터·전체 임베딩·OOM 방어까지 전부 해봤기에 "내보내도 된다"를 확신할 수 있었다. **부족한 자원을 인지하고, 측정으로 우선순위를 세워, 차이가 큰 곳에 배분하는 것** — 이번 결정의 가장 큰 수확이며, 온라인/로컬 양쪽을 준비해 선택 운영한다는 서사는 코드(포트 뒤 어댑터 + .env 스위치)로 그대로 증명된다.

**⑥ insight 30일 보관 정책·시연 QA (BE v0.15.8~v0.15.9 / FE v0.27.10)**

- **30일 보관 정책(스펙 §9-1 개정)**: 리포트 "즉시 파기" → **생성 +30일 보관, 본인 연장 30일×최대 2회(총 90일), 이후 무조건 파기**(접근 시 lazy purge 410 + start 시 전역 sweep — 스케줄러 불요). 리셋(`DELETE /play-records`)에 insight 파기 배선(Composite 패턴, 실측 세션 6·슬롯 63 → 0·0). FE는 보관 안내 배너 + 연장 버튼(마지막 연장 confirm 노티, 소진 시 삭제 예정 문구).
- 시연 QA 2건: 재권유 문구를 `{label}` 템플릿으로 전환(12슬롯 재권유 12종 전부 상이 — 동일 문구 반복 체감 해소), **최저임금 미달 룰 플래그** — 진술 시급 < 10,320원이면 쟁점 확인 요청에 표기. 한글 수사 파서("만원"/"9천" 정규화 편차 흡수), 문구는 학생 진술값 그대로 인용(임의숫자 게이트 정합). 테스트 66건 통과.

**⑦ 방어전 상호 대응 대사 + 자연어 신뢰성 배터리 (BE v0.15.10~v0.15.11)**

- **Phase 1 상호 대응 대사**: 판정 포트·프롬프트는 무변경(평가 100% 실적 보존), 대사 선택만 결정론 룰로 — 클리어 논거별 변형(교부/벌금·시점/수단·신고) + 실패 유형별 전용 리액션(협박/질문/화제이탈/철지난 논거) + 표정 매트릭스. `NPC_SPEECH_PROVIDER=seed→gemini` 전환 — 태그로 고른 시드 대사를 학생 발화 맥락으로 말투만 변형(실패 시 시드 폴백), 턴 1.6~3.5s. 회귀 테스트 12건은 실플레이 발화를 픽스처로.
- **75발화 자연어 신뢰성 배터리**(Fable 5 직접 플레이 평가): 반말·오타·감정·항복·조건부 거부·엣지 10종(욕설·이모지·무의미 문자열·조문 복붙 등)으로 1→3선 관통 3회 — **위험 오판정(오CLEAR) 0, 대사 정합 50/55(91%)**. 발견 반영: `GIVE_UP`(항복 낚아채기)·`TELL`(어른에게 이르기) 태그 신설, 조건부 거부 가드("안 써주면 일 안 할래요"는 협상이지 항복이 아님), 항복 시에도 반말 유지(캐릭터 붕괴 방지 톤 가드).
- **`NONSENSE` 태그(2층 시맨틱 분류 완성)**: 맥락 토큰이 전무한 잡담에 "뭔 소리야~ + 화제 복귀" 전용 대사. 맥락없는 발화 20종 배터리(날씨·게임·MBTI·로또 등): 판정 20/20 전부 ONGOING(오CLEAR 0), 대사 정합 18/20. quest 테스트 62건 통과.

**⑧ Guard 위반 검출 확장·UX 개편 (BE v0.12.28~v0.15.5 / FE v0.25.5~v0.27.8)**

- **위반 검증 시나리오 VIOL-01~05 완주**: 최저임금 미달(`MIN_WAGE_BELOW`)·필수 기재사항 누락, 연소근로자 주40시간 초과·야간근로·야간수당 거부(`ART69/ART70/ART56`), 위약금(`손해배상액`, OCR 오인식 `위익금`까지 매칭)·상품권 지급·연소자 증명서 미비, 휴게 미부여·포괄임금 가산거부·임금 공제 — OCR 플래그·룰·시드/마이그레이션 4건. CLEAN 계약서 ART66 오탐(서식 상시 라벨을 미비로 오인) 수정.
- **UX 개편 — 촬영 직행**: 문서 종류 선택 화면 제거, 업로드 `doc_type=AUTO`(OCR 제목 판별 + 본문 기반 자동 교정 — 계약서를 급여명세서로 올려 `hourly_wage=19`가 되던 오인식 해소). OCR이 종류·필드를 읽으면 **업로드 한 요청에서 진단 리포트까지 반환**(FE 2차 PATCH 생략), 고신뢰 시 확인 화면 생략·자동 진단.
- FE 연쇄 수정: React Strict Mode 이중 OCR로 Paddle 워커가 죽던 "서버는 성공·화면은 500" 문제(1회 실행 + 55초 타임아웃 Route Handler 프록시), 발견 항목 한글 라벨 전면 정비(최저임금 미달·필수 기재사항 누락 등).

**⑨ 게임 월드·UI 폴리시 + 잠금 우회 버그 풀스택 수정 (FE v0.21.1~v0.27.7 / BE v0.15.3)**

- **잠긴 퀘스트 우회 버그**: 하이드레이션 전 노드 진입 보완 경로(FE v0.27.7) × 세션 생성 시 해금 순서 미검증(BE v0.15.3, `QUEST_LOCKED` 409 게이트 신설) 두 층을 함께 막아 "포기 후 노드2 열림" 증상 근본 해결.
- **Modern UI Style 2 스킨 전면 적용**(v0.25.0~v0.26.2): 튜토리얼 모달 → 시나리오 대화창 → 전투 화면 전체를 크림 라운드 9-slice 패널로 통일 — 프레임 스타일을 `shared/ui/uiSkin.ts`로 승격(feature 간 직접 import 금지 규칙), 면 밝기 변형 3종(기본 크림/순백 입력창/어두운 크림 버튼).
- **조작 튜토리얼**(v0.24.0~v0.24.3): 플레이 기록 없으면 허브 진입 시 키캡 UI 안내, '이동' → '포탈·대화' 2단계 진행. **허브 헤더 스탯 수치 3종**(v0.22.0, localStorage 스냅샷) + 랜딩 로그인 상태 연동(v0.26.0~v0.26.1, 미로그인 PLAY 차단 notice·로그아웃 버튼).
- 월드 디테일: 차량 2대 추가·속도 통일(150px/s)·간격 불규칙화, 나사장 클리어 후 후일담 대화(전투 재진입 대신 2줄 안부), 대화 키 고착 무한 루프 수정(`resetKeys`), 편의점 남벽 반 칸 통행, 화면 하단 남색 띠 제거(최소 줌 보정), **공원 밀도 보강 13종**(아기천사 석상 쌍·꽃 6종·새집 2종·꽃수레 — 코드 변경 없이 콘텐츠 배치만).

**⑩ 다음 단계**

- 골든패스 리허설 사이클, 구어 추출 보강(실인터뷰 발견 개선), DOC 미니게임, 구글 OAuth 실연동.

## 2026-08-28 — 판정 신뢰성 총력전·챗봇 UX 전환의 날: 평가셋 156케이스 전 게이트 통과 + Guard 35변형 회귀 35/35 + 도배·날조 방어 + 가드·리포트 챗봇 패널 전환 + 주휴수당 규칙·계산기 개편 (BE v0.15.12→v0.16.15 / FE v0.27.11→v0.30.21)

자정 직후 전일 작업분 3커밋(guard v0.15.5 / insight·quest v0.15.6~v0.15.12 / frontend v0.27.6~v0.27.10)을 main에 릴리스하며 시작해, 하루에 백엔드 19개·프론트 33개 버전. 오전은 평가셋 실전화·Guard 회귀 하네스·문서 정합(ERD v2.0), 낮부터는 방어전 판정의 오CLEAR 사냥과 insight 도배(같은 말 반복 입력)·날조 방어, 가드·상담리포트 진입을 챗봇 위젯·패널로 바꾸는 UX 대전환(수호천사 캐릭터 배제 확정), 저녁에는 주휴수당 미니게임의 획득 규칙 개편과 계산기 UI 리디자인(프론트 폴리시는 신채연 담당 구간 포함)으로 이어졌다.

**① 구어 추출 보강 + 평가셋 실전화 146→156케이스 (BE v0.15.12)**

- 실인터뷰 점검에서 발견된 구어 추출 실패의 개선 사이클 완주: 추출 프롬프트에 **구어 규칙 4종**(한글 수사도 값·추정/자문은 APPROX·다중 항목 쉼표 병기·압축 금지) + "재직 중=NONE" 시맨틱 + few-shot 3종(평가셋과 겹치지 않는 신규 예시 — 과적합 방지).
- `COLLOQ` 그룹 8케이스 — 실패 발화 원문 그대로 회귀 고정(추출 70→78). **`REPLAY` 페르소나 2건(Historical Replay)** — 실세션(사용자 PDF 세션·자연어 세션)을 E2E 재생, 산출물 단언(최저임금 쟁점·목표 압축 방지·증거 병기·재직중 NONE) + 재권유 문구 중복 게이트. 슬롯 키 답변(`answers_by_slot`)으로 선충전 변동에 강건.
- **gemma 156케이스 재평가 전 게이트 통과**: 분류 **100%**(68/68)·urgent 누락 0 / 값 **90.2%**·precision **89.1%** / **치명 0** / 페르소나 **10/10**(리플레이 2 포함) / COLLOQ 8/8.
- 보너스 수확: 평가 러너의 `_MemSessions`가 30일 보관(v0.15.8) 포트 확장 누락(`purge_expired`)을 검출 — **평가 하네스가 리팩터링 회귀 안전망으로 동작**한 실례.

**② Guard OCR·룰셋 35-variant 회귀 하네스 (BE v0.15.13)**

- 위반 계약서 5종 + 깨끗한 계약서 2종 × 촬영 변형 5종(정상 스캔·저해상도 JPEG·기울임·접힘·저조도) = **35케이스 전수 기대 finding 고정** — `guard_sample_expectations.py` + 픽스처 + `run_guard_sample_eval.py`(마크다운 리포트) + `@pytest.mark.paddle` 회귀 테스트.
- 실측 **35/35 통과**. 접힘·저조도 엣지에서 발견된 OCR 붙어읽기(`휴게시간:없음`, `그전액을 익월`)는 정규식 보강으로 흡수 — 시연 당일 촬영 품질이 흔들려도 진단이 흔들리지 않는다는 근거 확보.

**③ 문서 정합 — 코드가 앞서간 만큼 문서를 당겨오기**

- **ERD v2.0 전면 개정**: v1.0(8/3 설계 초안)을 **구현 스키마 기준(ORM 원본, 23테이블·5 BC)**으로 재작성. v1.0의 `violation_type` 온톨로지 허브 구상 대비 실제 구현의 차이를 정직하게 기록 — insight BC는 consult 5테이블 설계를 팩/세션 2층 4테이블로 재설계(질문·쟁점·팁·라우팅을 `insight_pack_item` 데이터 행으로 — 콘텐츠 추가 시 스키마 불변), 역정규화 2건(law_chunks 임베딩 2쌍·guard_finding.rule_version)에 근거 명시, 미구현 테이블군은 P2 이월로 표기. 지킬 ERD 페이지 동일 반영.
- **`metrics_status.md` 소급 반영**: 8/27 실측이던 방어전 자연어 75발화 배터리(오CLEAR 0)·맥락없는 발화 20종(20/20 ONGOING)을 판정 시계열에, Call 2 대사 모델 선정(gemini 채택·**gemma 기각 근거 +2s 실측**)과 대사 정합 91%/90% 검증을 발화 파이프라인에, OCR 엔진 선정 근거 4항(개인정보·VRAM 배분·VLM 배제·provider 스위치)을 Guard 섹션에 편입 — "기각한 후보도 실측 근거가 있다"로 모델 선택 서사 완결.

**④ 방어전 판정 정합성 스프린트 — 오CLEAR 사냥 (BE v0.15.14→v0.16.3·v0.16.9~v0.16.15 / FE 판정 동기 6건)**

어제 75발화 배터리가 남긴 숙제를 하루 종일 갚은 구간. 판정이 흔들리는 지점을 하나씩 재현→차단하며 백엔드 판정 버전만 12개를 쌓았다.

- **판정 견고화(v0.15.14)**: 문장 끝 마침표 유무로 CLEAR/ONGOING이 갈리던 흔들림 정규화, **동일 문장 전단계 통과 차단** — LLM CLEAR는 현재 방어선 키워드 hit일 때만 유효(1단 논거를 2·3단에서 재탕하면 PREV_ARG 리액션), 2단 「시작 전」 오클리어 게이트, CLEAR·HINT 판정 시 시드 항복 대사를 Gemini가 「나중에 쓰자」 톤으로 되돌리던 문제 수정.
- **대사 정합 3건(v0.16.1~v0.16.3)**: 2단 오프닝 「쓸게, 써」 작성 동의 뉘앙스 제거(미루기 톤 시드 교체), 「고용노동부에 신고할거예요」가 애매 발화로 오매칭돼 「어디에 신고할 건데?」가 나오던 문제(기관명 동반 시 예외 + LABOR_THREAT 항복 리액션), LLM이 HINT를 내려도 키워드 CLEAR hit이면 반박 성공 연출 우선.
- **3단 신고 판정의 하루 여정(v0.16.4→v0.16.9→v0.16.10)**: 기관+신고만 있는 발화를 「아쉬운 승리」(INCOMPLETE_CLEAR — 클리어는 되지만 보상 축소)로 신설했다가, 비활성(클리어 불가)으로 뒤집고, 최종적으로 **FULL CLEAR 확정** — 고용노동부에 신고하겠다는 선언 자체가 교육 목표상 유효한 반박이라는 판단. 프론트 MSW 판정기도 3회 함께 동기(v0.30.7→v0.30.14→v0.30.16).
- **혼합 발화 Veto(거부권 — 정답 키워드에 걸려도 틀린 사실이 섞이면 통과를 막는 장치)**: 「맞는 논거 + 근로자가 벌금 낸다는 오개념」이 키워드 CLEAR로 통과하던 구멍을 `wrong_fact_veto.py`로 차단(v0.16.13, eval 39건 회귀 통과), 사장님 귀속 벌금(「벌금 내셔야」)까지 오차단하던 false positive 제외(v0.16.15). MSW `wrongFactVeto.ts` 동기(v0.30.17·v0.30.21).
- **EXAONE 판정 신뢰성 마감**: SLM이 약한 반박·항복에 HINT를 남발하던 문제를 키워드 교차 강등으로 해소(v0.16.12) — **EXAONE eval 54건 100%·오CLEAR 0**. 함정 19건(오CLEAR 유도·방어선 미스매치·항복·조문 복붙) + NPC 대사 문맥 10건의 **방어전 정합성 평가셋**을 상시 회귀로 편입(v0.16.11).

**⑤ insight 도배 소프트 사다리 + 추출 날조 차단 (BE v0.16.0·v0.16.14 / FE v0.30.0)**

- **도배 대응 사다리(v0.16.0)**: 「몰라」×N 같은 무의미 반복 답변을 LLM 없이 휴리스틱으로 판정(`spam_guard.py` — 반복 토큰 비율·단일 문자 도배·직전 답변 동일 반복), 1회차 재질문 → 2회차 해당 슬롯 '모름' 확정 후 전진 → 3회차 60초 입력 쿨다운(429). 도배 세션의 LLM 소모 **~15회 → 0회**. 도배 원문이 인쇄물 한 줄 요약에 그대로 실리던 어제의 유일 잔여 이슈도 고정 문구 폴백으로 마감. 경계 테스트 12케이스(짧은 정상 답변 「몰라」·「없어요」 미검출 포함). FE는 쿨다운 안내 메시지 표시(v0.30.0).
- **추출 날조 차단(v0.16.14)**: 발화·허용 소스에 없는 숫자·한글 토큰을 UNKNOWN으로 강등하는 `value_fabrication` 검증을 추출기에 상시 장착 — **EXAONE 치명 1→0**. TRAP few-shot 보강(헤지·추정 표현은 null) 후 gemma 재평가: **분류 100%·값 96.7%·precision 95.7%·치명 0·함정 전수 통과** — 어제 92.9%에서 재상승.

**⑥ 가드·상담리포트 챗봇 UX 대전환 — 수호천사 배치 → 당일 철회 (FE v0.28.0→v0.29.0)**

- 오전 v0.28.0에서 수호천사 컴포넌트(`AngelHost.tsx` — 부유 스프라이트·대기 연출·말풍선 타자 효과)를 가드·리포트 9화면에 배치했으나, 저녁 방향 결정으로 **가드·상담리포트에서 캐릭터 요소 전면 제거**(v0.29.0 — 헤더 스프라이트·채팅 아바타·화자 라벨 삭제, 로딩은 스피너 복귀) — 신뢰 문서를 다루는 두 기능은 **흰색 유틸리티 톤 + Pretendard**로 확정하고 캐릭터는 게임(전투 펫)에만 남김. 하루 안에 만들고 걷어냈지만 결정 근거가 남았다.
- **진입 방식 전환**: 헤더 버튼·별도 페이지 이동 대신 허브맵 좌하단 **챗봇 위젯**(파란 필 버튼 2개, v0.28.2) → 클릭 시 좌하단 **챗봇 패널**(440×680, 블루 그라데이션 헤더, Esc/✕ 닫기, v0.29.0)로 기존 가드 4화면·리포트 5화면을 그대로 이식. 패널이 열린 동안은 캐릭터 이동 차단, 랜딩에서도 로그인 상태면 동일 위젯 노출.
- **리포트 인터뷰를 실제 메신저 대화창으로**: 자유서술 첫 화면부터 채팅 UI(질문 말풍선 + 라운드 입력바 + Enter 전송), 인터뷰는 좌우 말풍선·퀵리플라이 칩(잘 모르겠어요/그런 거 없어요), 안내·에러·버튼 문구 전면 해요체 통일(전투 NPC 반말은 게임 연출로 유지).
- **Vercel 빌드 차단 해소**(v0.28.0): `GuardConfirmScreen` 재분류 응답 union 내로잉 + null 가드로 타입에러 7건 전부 수정, 정크 0바이트 파일 2개 삭제 — `npx tsc --noEmit` 0건·`next build` 통과.
- 폴리시 3건: 계산 던전 키패드에 사칙연산·`=` 버튼(v0.27.11) 후 키보드 입력 전면 비활성(v0.28.1 — 화면 키패드로만), 게임 UI 텍스트 드래그 선택 방지(v0.27.12), 나사장 재도전 시 인사 생략 바로 전투(v0.27.13).

**⑦ 랜딩·인증 개편 — 로그인 모달 + 랜딩 = 진짜 허브 (FE v0.28.2·v0.29.0·v0.29.3)**

- **로그인·회원가입 모달**: 별도 /login·/signup 페이지를 폐지하고 랜딩 위 크림 패널 모달로(라우트는 `/?login=1` 리다이렉트만 유지). 구글 버튼의 4색 G 로고는 외부 CDN 금지 원칙에 따라 인라인 SVG 자체 렌더. 어트랙트 모드(입력 없이 배경으로 도는 데모 화면)의 Phaser가 WASD·Space를 전역 캡처해 모달에서 a/s/d 타이핑이 안 되던 문제는 `clearCaptures()`로 해제.
- **로그인 랜딩 = 실제 허브 화면**: 랜딩 뒤가 목업이 아니라 진짜 허브(주인공·실제 노드 상태·HUD 포함)가 되고, PLAY는 라우팅·암전 없이 **오버레이만 400ms로 걷히며** 같은 게임 인스턴스에서 곧바로 플레이. 전투에서 브라우저 뒤로가기 시 맵 위에 랜딩 오버레이가 다시 뜨던 버그는 sessionStorage(브라우저 탭 단위 임시 저장소) 플래그로, 로그아웃 랜딩에 이전 계정 진행도가 비치던 문제는 어트랙트 모드 캐시 미반영으로 각각 수정. `game.destroy()` 경로에서 죽은 씬의 EventBus 리스너가 일으키던 크래시도 가드.
- **실백엔드 연동 보강(v0.29.3)**: `POST /quest-sessions` 404를 catch-all 프록시(모든 API 경로를 백엔드 8100으로 넘기는 중계 라우트)로 해소, 로컬 `.env.local`은 실백엔드 모드(`FALLBACK_MODE=0`)로 — 목 시연은 `npm run dev:mock`(3101) 분리.

**⑧ 주휴수당 CALC — 획득 규칙 개편 + 계산기 리디자인 + 살아있는 배경 (BE v0.16.5~v0.16.8 / FE v0.28.2~v0.30.21)**

- **획득 규칙의 하루 여정(BE)**: 오답 허용 진행(5문항 전부 제출 시 클리어, 차액 차감 — v0.16.6) → **정답 문항 전액/오답 0원 확정**(차액 차감 폐지, v0.16.7) → 허브 「지켜낸 돈」이 카탈로그 고정값(330,240원)이 아니라 **실제 정답 문항 합산액**을 표시(v0.16.8). FE도 오답 후 「다음 문제」 진행·획득 규칙 안내 동기(v0.30.9·v0.30.10).
- **플레이 기록 제거 범위 선택**: `DELETE /play-records?scope=all|calc`(v0.16.5) — 미니게임만 초기화하고 퀘스트·가드·리포트는 유지. FE는 모달에서 「모든 기록」/「주휴수당 계산만」 선택(v0.30.8).
- **계산기 리디자인**: 키패드 겹침을 4행 그리드로 해소(v0.30.1) → 화면비 무관 67:33 비율 고정(v0.30.2~v0.30.4) → `00`·`05` 입력이 거부돼 정답 제출이 막히던 문제(strict-mode 숫자 리터럴 거부 → `Number` 정규화, v0.30.6) → 3열 숫자 전용 실험 후 아이폰식 4열(÷×−+=) 복원(v0.30.19) → 문제 카드를 전투·대화창과 동일한 크림 9-slice 패널로 통일(v0.30.20).
- **살아있는 배경**: 정지 이미지 대신 어트랙트 허브맵을 배경으로 깔고 카메라를 벤치의 친구에게 줌 고정 — 맵 속 실제 친구가 주인공이 되고, 스프라이트 시트에서 재크롭한 물방울 말풍선 6프레임(맺힘→낙하)을 머리 위에 루프 재생(v0.28.2). 시각 확인을 거치며 줌 배율·앵커를 수차례 미세 조정(v0.30.18 등).
- 버그 2건: 미니게임 클리어가 허브 포탈을 파랗게 바꾸던 표시 분리(v0.30.11), 토큰 갱신 시 세션이 재시작돼 Q5 클리어 후 Q1로 돌아가던 부트스트랩 가드(v0.30.12). 스테이지 2·3 포탈은 「준비 중」 안내로 통일(v0.30.13·v0.30.15).

## 2026-08-29 — 리허설의 날: 자동 완주 3라운드 9회 + 폴백 훈련 2종 실발동, 결함 6건 발견·수정 (BE v0.16.16→v0.16.18 / FE v0.30.22→v0.30.25)

전일 작업분 2커밋(BE v0.15.13~v0.16.15 / FE v0.27.11~v0.30.22)을 main에 올리며 시작. 오늘은 새 기능을 만드는 날이 아니라 **만든 것이 실제로 도는지 확인하는 날**이었다. 브라우저 자동화로 전체 동선(로그인→방어전→주휴수당 계산→가드→상담리포트)을 3라운드 9회 주파하고, 장애 대응 절차를 처음으로 실제 발동했다. 오늘 나온 코드 변경은 전부 리허설이 찾아낸 결함이다.

**① 전일 릴리스 + 테스트 회귀 4건 수정 (BE v0.16.16)**

- 커밋 정리 중 백엔드 테스트 4건 실패를 발견. 둘 다 "테스트가 프로덕션 변경을 못 따라간" 유형이었다.
- insight 3건: v0.16.14 날조 차단(`value_fabrication`)으로 발화에 없는 값은 UNKNOWN으로 강등되는데, 테스트 대역(`FakeInsightLlm`)이 여전히 발화에 없는 연도(`2026년`)를 붙여 반환하고 있었다. 대역을 실제 정책에 맞춰 수정.
- guard 1건: 합성 이미지에 한글을 그리는데 **PIL 기본 폰트에 한글 글리프가 없어** 라벨이 아예 렌더링되지 않았다. 시스템 CJK 폰트를 쓰도록 수정(없으면 skip).

**② 시드를 고쳐도 DB에 닿지 않던 구조적 결함 (BE v0.16.17)**

- 2단계 NPC 오프닝이 v0.16.1에서 미루기 톤으로 교체됐는데 **실제 DB에는 구버전이 그대로 있었다.** 작성에 동의해놓고 곧바로 미루는 모순 대사가 시연에 노출될 뻔했다.
- 원인: `ensure_seeded`가 퀘스트 존재 시 early return하고 키워드만 동기화해, **대사 변경은 기존 DB에 영원히 반영되지 않는** 구조였다. L1·L3은 우연히 일치했고 L2만 어긋나 있어 더 늦게 드러났다.
- 동기화를 `_sync_defense_content`로 확장해 `npc_opening`·`npc_clear`·`ongoing_lines`·`hint1/2`·`evidence`까지 시드 기준으로 갱신. 시드가 단일 진실 원천이므로 이제 대사 수정이 재기동만으로 반영된다. DB 없이 세션 스텁으로 검증하는 테스트 3건 추가.

**③ 상담리포트 문구 해요체 통일 (BE v0.16.18)**

- 프론트 v0.29.0에서 UI 문구를 해요체로 통일했으나 인터뷰 질문·증거 안내·긴급 안내는 **초기 시드 마이그레이션에 데이터로 박혀 있어** 변환되지 않았다. 같은 채팅창에서 질문은 반말, 시스템 안내는 해요체로 섞여 보였다.
- alembic `c2d3e4f5a6b7`로 슬롯 질문 15종·증거 팁 근거 7종·REASK 문구·`evidence_notice`(12팩)·`urgent_notice`(4종)를 변환. 긴급·증거 안내는 **프론트 폴백에 이미 있던 해요체 원문과 문구까지 일치**시켜 서버 값이 뜨든 폴백이 뜨든 동일하게 했다. downgrade 왕복 검증 완료.

**④ 프론트 결함 3건 (FE v0.30.23~v0.30.25)**

- **미니게임 클리어 후 허브 스탯이 기본값으로 회귀**(v0.30.23): 스탯 스냅샷이 `skipHydration` persist인데 미니게임 화면은 rehydrate를 부르지 않아, 클리어 시 메모리의 `null`이 저장본을 덮어썼다. 마운트 시 rehydrate 추가.
- **「정답 제출」 버튼 하단 잘림**(v0.30.24): 키패드가 `aspect-square`라 계산기 높이가 **폭으로만** 정해지고 가용 높이를 무시했다. 뷰포트 높이 745px 이하에서 래퍼 밖으로 밀려 잘렸다(700px에서 45px 초과). 키패드가 남은 높이를 채우도록 바꾸고 높이 상한을 둬 600~1060px 전 구간 잘림 0.
- **계정 간 스탯 스냅샷 유출**(v0.30.25): 스냅샷이 로그아웃 후에도 남아 **다른 계정으로 로그인하면 첫 배틀 전까지 이전 사용자 수치가 보였다.** 요구가 둘이었다 — 점수는 계속 쌓이니 유지돼야 하고, 다른 계정엔 보이면 안 된다. 로그아웃 시 일괄 삭제하면 후자만 만족하고 전자를 잃으므로, **스냅샷에 소유자 계정을 함께 저장하고 계정 불일치면 무시**하는 방식으로 둘 다 만족시켰다. 소유자 없는 구버전 스냅샷은 자동 무시.

**⑤ 리허설 3라운드 — 9회 주파**

- 1차(3회): 위 결함 4건 발견. 2차(3회): 수정분 유지 확인 + 신규 1건(계정 간 스냅샷). 3차(3회): **무결점 · 콘솔 에러 0 · 신규 결함 0.**
- 3차에서 처음 검증한 경로 — 가드 정상 문서 「이상 없음」 판정, 가드→리포트 연계 이동, 로그아웃→같은 계정 재로그인 시 점수 즉시 복원, 재플레이 연습 클리어(점수 미기록), 도배 사다리 3단·쿨다운 입력 차단.
- 자동화 교훈 하나: 클릭 좌표를 스크린샷이 아니라 `getBoundingClientRect`로 잡아야 한다. 창 크기에 따라 스크린샷이 뷰포트의 축소본이 되는데, 이를 보정하지 않아 1차 라운드에서 "버튼이 안 눌린다"는 오판을 여러 건 만들었다.

**⑥ 폴백 훈련 — 절차에 빠진 단계를 찾다**

장애 대응 표를 만들어만 두고 한 번도 발동해본 적이 없어 두 시나리오를 실제로 돌렸다.

- **판정 LLM 사망**: `OLLAMA_BASE_URL`을 죽은 포트로 돌려 연결 실패를 재현. 키워드 판정 폴백으로 CLEAR·ONGOING 모두 정상, 조문 카드도 정상. 응답 **0.55~1.2초**로 오히려 평소보다 빨랐다(LLM 호출이 없어서).
- **오프라인 전환**(`EMBEDDING_PROVIDER=local`·`NPC_SPEECH_PROVIDER=seed`): 전환 자체는 28초에 끝났지만 **결과 상태가 시연 불가**였다. 로컬 임베딩이 백엔드 프로세스에서 GPU 8~10GB를 점유해 판정·리포트 모델이 오프로딩되고, 판정이 1턴 **22초**·이후 **10초**가 됐다. 리포트 모델을 내려 VRAM을 방어전에 몰아주자 **3.8초**로 회복 — 임베딩이 네트워크가 아닌 로컬이라 평소(5~8초)보다도 빠르다.
- 결론: **세 모델을 한 장에 다 올릴 수 없다**는 원래 설계 결론(그래서 임베딩을 외부로 뺐다)이 오프라인 폴백에서 그대로 되돌아온다. 오프라인에서는 방어전·가드까지 가능하고 리포트는 포기해야 한다. 가드는 OCR·룰 13종·조문 매핑이 전부 로컬이라 **네트워크도 LLM도 쓰지 않아** 영향이 없다.

**⑦ 판정의 한국어 표현 한계 규명 (현행 유지 결정)**

- 사람이 직접 플레이하다 2단계에서 막혔다. 의미상 맞는 문장인데 ONGOING이 나온다.
- 원인: 판정은 키워드 → 게이트 → LLM 3층인데 결정권이 앞 두 층에 있다. 2단 게이트가 "시기 언급 + 1단계 어휘 없음"을 요구하는데 그 어휘 목록에 **범용 동사 `써야`**가 들어 있어, 「…써야 해요」로 끝나는 문장이 전부 막혔다. exaone이 CLEAR를 내려도 키워드가 아니면 버려지는 구조라 LLM이 구제하지도 못한다.
- 개선안(게이트 규칙 완화 + 키워드를 개념 집합으로 통합, 시드·게이트 이중 정의 해소)을 설계했으나, 기존 정합성 평가셋의 함정 케이스가 현행 규칙을 못박고 있어 **테스트 수정이 아니라 판정 정책 변경**에 해당한다. 시연 전 회귀 위험을 피해 **현행 유지·시연 후 재검토**로 결정했다.

**⑧ 다음 단계**

- 발표 리허설(사람이 말하면서 10분 완주) — 기능 완주·폴백 훈련 게이트는 통과했고 이것만 남았다.
- 판정의 한국어 표현 인정 범위 확대(⑦ 개선안) — 시연 후.
- 랜딩 오버레이와 조작 튜토리얼이 겹쳐 보이는 문제 — 블로커가 아니어서 보류.

---

---

### 한눈에 보는 마일스톤

| 기간 | 단계 | 산출물 |
|---|---|---|
| 6/27 ~ 7/10 | 기획·설계 | 사업계획서·개발/UI 정의서 v1.2, 시스템 다이어그램 |
| 7/28 ~ 8/1 | 본선 대응 | 통합 정리서, 콘텐츠 설계서 A1~A5, 5주 실행 계획 |
| 8/2 ~ 8/4 | 백엔드 스프린트 | core 인프라 + ontology/quest/npc BC, 법령 1,593청크 × 2벡터 공간, 판정 API, ERD v1.0 |
| 8/23 | 프론트 스프린트 | 스캐폴딩 → API 계약/MSW → 방어전 → 허브맵 → 결과 화면 (v0.1.0→v0.6.3) |
| 8/24 | 풀스택 관통 | 인증(auth BC + auth feature) → 게임 API 이관(quest BC, MSW 제거) → 2-Call 판정(EXAONE+RAG, 왕복 3.05초) → Guard OCR(PaddleOCR + `/guard` 플로우) — BE v0.10.0→v0.12.15 / FE v0.7.0→v0.8.17 |
| 8/25 | 게임 월드 대확장 | CALC 던전 풀스택(주휴수당 Q1~Q5, 지켜낸 돈 330,240원) + 판정 평가 100%·오CLEAR 0 + 전 BC myself 프로브 → 허브 오픈월드(A* 이동·차량·신호등) → 편의점 실내 씬 + dev:mock → 포탈·시나리오·전투 UI 리뉴얼 — BE v0.12.16→v0.12.23 / FE v0.9.0→v0.20.27 (92개 버전) |
| 8/26 | 폴리시·연출 | 신호등 횡단보도별 독립 교통(가속 탈출·정지선·차간 클램프) → 편의점 벽 링·전투 연출(나사장 스프라이트·수호천사 펫·반박 성공 시퀀스) → 상태 반응형 포탈·세션 안정화(MSW 리로드 튕김 해결) → Guard DOCX 풀스택·OCR 정확도 → 랜딩 라이브 허브맵·전체화면 모드 + insight BC 설계 v2.2 — BE v0.12.24→v0.12.27 / FE v0.20.28→v0.20.52 (25개 버전) |
| 8/27 | insight 풀스택·AI 신뢰성 평가 | 상담 준비 리포트 풀스택(insight BC 4+1 API·엔진 4모듈 → FE `/report` → 실백엔드 전환·30일 보관) + 평가 하네스 146케이스·3모델 교차 평가(gemma3:12b 확정 — 분류 100%·값 92.9%·precision 91.7%·날조 0) + 방어전 상호 대응 대사·75발화 배터리(오CLEAR 0) + Guard VIOL-01~05·AUTO 자동 진단 + 임베딩 gemini 전환(VRAM LLM 전용, exaone+gemma 동시 상주 14.0/16.3GB) + Modern UI 크림 패널 스킨·시연 문서 3종 — BE v0.12.28→v0.15.11 / FE v0.21.0→v0.27.10 (78개 버전) |
| 8/28 | 판정 신뢰성 총력전·챗봇 UX 전환 | 릴리스 3커밋(main) + 평가셋 146→156 전 게이트 통과(분류 100%·값 90.2%→날조 차단 후 96.7%·치명 0) + Guard 35변형 회귀 35/35 + 방어전 오CLEAR 사냥(EXAONE 54건 100%·혼합 발화 Veto·3단 신고 FULL CLEAR 확정) + insight 도배 사다리(LLM 소모 ~15회→0회) + 가드·리포트 챗봇 위젯·패널 전환(수호천사 배제·메신저 대화창·해요체) + 로그인 모달·랜딩=실제 허브 + 주휴수당 전액/0원·계산기 리디자인·라이브 배경 + ERD v2.0(23테이블)·metrics_status 소급 — BE v0.15.12→v0.16.15 / FE v0.27.11→v0.30.21 (52개 버전) |
| 8/29 | 리허설·안정화 | 자동 완주 3라운드 9회(3차 무결점·콘솔 에러 0) + 폴백 훈련 2종 실발동(절차 결함 발견 — 오프라인 전환 시 리포트 모델 언로드 필수, 판정 22초→3.8초) + 결함 6건 수정(테스트 회귀 4건·시드 대사 DB 미동기화·문구 해요체·스탯 persist 회귀·계산기 버튼 잘림·계정 간 스냅샷 유출) + 판정 한국어 표현 한계 규명(현행 유지 결정) — BE v0.16.16→v0.16.18 / FE v0.30.22→v0.30.25 (7개 버전) |

---

### 스프린트 상태 (2026-08-30 기준)

> 일정 로드맵의 스프린트 표(Sprint 5·6)와 칸반 보드는 이 표를 기준으로 반영한다.
> 상태 근거는 커밋·버전 로그·위 일자별 기록뿐이며, 이 표를 갱신하지 않은 채 로드맵만 고치지 않는다.

**Sprint 5 — 통합·폴리싱 (8/25 ~ 8/31)**

| 담당 | 업무 | 상태 | 근거 |
|---|---|---|---|
| 이은상 | 게임 플레이 API 백엔드 이관 — quest BC 오케스트레이션, `quest_session`/`player_stat` 영속, MSW 제거 | ✅ 완료 (8/24) | BE v0.10.0→v0.12.15 / FE v0.7.0→v0.8.17 |
| 이은상 | 판정 평가셋 실측 → 판정 프롬프트 튜닝, 오입력 내성 케이스 처리 | ✅ 완료 (8/27~8/28) | 146→156케이스 전 게이트 통과, 오CLEAR 0, 도배·날조 방어 |
| 신채연 | 계산 미니게임(주휴수당 던전) 화면·로직, 신규 캐릭터 에셋 | ✅ 완료 (8/25, 8/28 규칙 개편) | CALC 던전 풀스택 Q1~Q5, 전액/0원 규칙 + 계산기 리디자인 |
| 신채연 | 연출 폴리싱 — 표정 전환, 응답 스트리밍, 클리어 사운드/모션 | 🔄 부분 | 표정 매트릭스·전투 연출 완료(8/26~8/27) / **응답 스트리밍·사운드 미착수** — 오디오 에셋 0건 |
| 김충식 | 선택지 시뮬레이션(부당해고) 콘텐츠, 발표자료·시연 시나리오 | 🔄 진행 중 | 발표·시연 준비 진행 / 선택지 시뮬레이션은 계획상 의도적 보류(P1 여력분) |
| 전원 | 통합 리허설 — 완주 3회 + 오프라인 전체 데모 + 심사위원 직접 타이핑 시나리오 | ✅ 완료 (8/29) | 자동 완주 3라운드 9회(3차 무결점), 폴백 훈련 2종 실발동, 결함 6건 수정 |

남은 것: 응답 스트리밍·클리어 사운드(P1), 사람이 말하면서 하는 발표 리허설 10분 완주.

**Sprint 6 — 제출·대회 (9/1 ~ 9/2)**

| 담당 | 업무 | 상태 |
|---|---|---|
| 김충식 | 최종 결과물 패키징 — 제출물 점검, 발표자료 최종본 | ⬜ 예정 |
| 이은상 | 백엔드 안정화 — Fake↔실LLM 전환 스위치 최종 점검 | ⬜ 예정 (8/29 폴백 훈련에서 발견한 절차 결함 반영 필요) |
| 신채연 | 프론트 최종 빌드 — 시연 기기 세팅 확인 | ⬜ 예정 |
| 전원 | 해커톤 대회 — 발표 + 실제 플레이 시연, 최종 결과물 제출 | ⬜ 예정 |

**칸반 보드 이동 (8/24 기준 → 8/30 기준)**

| 이동 | 항목 |
|---|---|
| Backlog → Done | 실전 가드(계약서 AI 진단), 상담 준비 리포트 — P2 이월 예정이었으나 8/26~8/28에 풀스택 구현·회귀 검증까지 완료 |
| To Do → Done | 계산 미니게임(주휴수당 던전), 판정 평가셋 실측·오입력 내성, 통합 리허설·심사위원 타이핑 시나리오 |
| In Progress → Done | 게임 플레이 API 백엔드 이관(MSW 제거), 신규 캐릭터 에셋 파이프라인 |
| To Do 유지 | 선택지 시뮬레이션(부당해고) — P1 보류 / 최종 결과물 패키징(9/1) |
| In Progress 유지 | 발표자료·시연 시나리오 / 연출 폴리싱 — 잔여 범위는 응답 스트리밍·클리어 사운드 |
| Backlog 유지 | 2차 방어전 퀘스트 · 공유 카드 · 모바일 앱 |
